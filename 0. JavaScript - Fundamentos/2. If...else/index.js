var b = new Boolean(false);
if (b); // essa condição é avaliada como true
// qualquer objeto, incluíndo um objeto Boolean cujo valor é false, 
// é avaliado como true quando passado por uma condicional.


if (cipher_char === from_char) {
    result = result + to_char;
    x++;
 } else {
    result = result + clear_char;
 }

if (x > 5) {

} else if (x > 50) {

} else {

}
